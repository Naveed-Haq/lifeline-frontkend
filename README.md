# LifeLine Frontend (Vite + React)

## Quickstart
1. `npm install`
2. `npm run dev`

The frontend expects the backend to be proxied at `/api`. If running backend on a different port during development, configure Vite proxy or use full backend URL in axios.
